Initialize the demo presentation. Run these commands:

```bash
python3 demo/demo-steps.py reset
```

```bash
python3 demo/demo-steps.py start
```

The first command resets the step counter. The second starts a tmux session with presenterm on top and Claude Code below. The presenter types `/next` in the Claude pane to advance slides.
